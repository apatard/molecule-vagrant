---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: virtualbox
lint:
  /bin/true
platforms:
  - name: instance
    provider_options:
      default_nic_type: '"virtio"'
    box: ${TESTBOX:-centos/7}
provisioner:
  name: ansible
verifier:
  name: ansible
